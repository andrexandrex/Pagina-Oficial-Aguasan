name = "aguasan-whatsapp-worker"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# Secrets are set with `wrangler secret put <NAME>`, not here:
#   WHATSAPP_ACCESS_TOKEN
#   WHATSAPP_PHONE_NUMBER_ID
#   WHATSAPP_VERIFY_TOKEN
